Usage: kmeans [options] input_file
When input_file is not supplied, the program reads from stdin.

Allowed options:
  -o
  Output file name (stdout, if no output file is supplied)

  -action=[clustering|metrics]
  The program either computes a clustering a given graph or calculates a set of quality measures (aesthetics)

  -C=[geometrickmeans|graphkmeans|geometrichierarchical|graphhierarchical|infomap|modularity]
  Type of clustering

  -K
  Desired number of clusters (selected automatically, if no value is supplied)
